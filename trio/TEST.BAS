INCLUDE "GLOBAL_DEFS"
'
'BASE(1,2)
'DEFPOS(0,0)
'SPEED=1
'MOVE(1,1)
'WAIT IDLE
'MOVE(1,1)
'CANCEL
'MOVE(1,1)
'BASE(axis_u,axis_v)
'CANCEL
'BASE(axis_v)
'SPEED=5
'MOVEABS(0)
'WAIT IDLE
'BASE(axis_u)
'SPEED=5
'MOVEABS(0)
'WAIT IDLE
'STOP
DIM a AS INTEGER
IF IN(50)=ON THEN
    a=1
ELSE
    a=0
ENDIF

OP(50,ON)
IF IN(50)=ON THEN
    a=1
ELSE
    a=0
ENDIF

